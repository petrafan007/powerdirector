// AUTOMATICALLY GENERATED Documentation Component for broadcast
import React from 'react';

const BROADCAST_CONFIGS = [
    {
        path: 'broadcast.strategy',
        label: 'Strategy',
        type: 'Enum: local | redis | mesh',
        description: 'Cluster scaling architectural vector. Determines how real-time WebSocket messages (like Agent typing indicators or streaming text chunks) physically traverse the network when PowerDirector is scaled up across dozens of parallel Node.js servers spanning multiple data centers.'
    }
];

export default function BroadcastConfigDocs() {
    return (
        <div className="space-y-6 pb-24 max-w-[1200px]">
            <h1 className="text-4xl font-bold text-[var(--pd-text-main)]">Websocket Broadcasting Configuration</h1>
            <div className="prose prose-sm max-w-none border-b border-[var(--pd-border)] pb-8 mb-8">
                <p className="text-[var(--pd-text-main)] text-lg leading-relaxed opacity-90">Microservice-level scaling parameters. Controls the Pub/Sub messaging backplane guaranteeing that real-time conversational streaming synchronizes flawlessly across massively distributed stateless instances.</p>
            </div>
            <div className="space-y-6">
                {BROADCAST_CONFIGS.map((config) => (
                    <div key={config.path} id={config.path} className="bg-[var(--pd-surface-panel)] border border-[var(--pd-border)] p-6 rounded-xl shadow-sm hover:border-[var(--pd-blue-500)] transition-colors scroll-mt-24">
                        <h3 className="font-sans text-xl font-bold mt-0 mb-3 text-[var(--pd-text-main)]">{config.label}</h3>
                        <div className="flex flex-wrap gap-3 mb-4 text-xs font-mono opacity-80">
                            <span className="bg-[var(--pd-surface-hover)] border border-[var(--pd-border)] px-3 py-1.5 rounded">
                                Path: <span className="text-[var(--pd-text-main)] font-semibold">{config.path}</span>
                            </span>
                            <span className="bg-[var(--pd-surface-hover)] border border-[var(--pd-border)] px-3 py-1.5 rounded">
                                Type: <span className="text-[var(--pd-text-main)] font-semibold">{config.type}</span>
                            </span>
                        </div>
                        <div className="text-[0.95rem] text-[var(--pd-text-muted)] m-0 leading-relaxed font-normal">
                            <p>{config.description}</p>
                        </div>
                    </div>
                ))}
            </div>
        </div>
    );
}
